services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./src:/app/src:ro

    command: sh -c "exec uvicorn src.main:app --host 0.0.0.0 --port 8080 --reload --reload-dir src"
